pipeline {

  agent any 

    stages { 

        stage( 'Checkout') {

            steps {
                checkout scm

            }
        }

        stage( 'Build') {

            Steps {

                echo "Project download successfully."
            
             
            }
        }
    }
}